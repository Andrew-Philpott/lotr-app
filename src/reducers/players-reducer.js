// import * as a from "../actions/ActionTypes";
// let initialState = {
//   players: {}
// }

// export default (state = initialState, action) => {
//   let newState = null;
//   switch (action.type) {
//     case a.GET_PLAYERS:
//       return {
//         players: { ...state },
//       };
//     case a.NEW_PLAYER:
//       newState = {...state};
//       return {
//         players: newState[action.payload.id] =
//       }

//     case a.ATTACK:
//       newState = { ...state };
//       player = newState[action.payload.id];
//       player["health"] -= action.payload.attack;
//       newState[action.payload] = player;
//       return newState;
//     default:
//       return state;
//   }
// };
//       player = newState[action.payload.id];
//       player["health"] -= action.payload.attack;
//       newState[action.payload] = player;
//       return newState;
//     default:
//       return state;
//   }
// };
